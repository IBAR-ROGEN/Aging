# Legacy convenience pointer — do not treat this file as the dependency source of truth.
# Canonical dependencies and extras live in pyproject.toml.
#
# Install:
#   uv sync --extra dev
#
# Avoid `pip install -r requirements.txt` for day-to-day work; use uv so the lockfile
# (uv.lock) stays authoritative.
